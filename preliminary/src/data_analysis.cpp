#include <bits/stdc++.h>
#include "debug.h"
#include "io.h"
#include "wrong.h"
#include "object.h"
#include "disk.h"
//#include "segment_tree.h"
#include "reward_query.h"
using namespace std;
template<typename T>
ostream& operator<<(ostream& os, const vector<T>& nums) {
    for(const T& num: nums) os << num << " ";
    return os;
}




int main() {
    ifstream file("data.txt");

    vector<vector<int>> state;
    vector<double> profit;  
    


    return 0;
}